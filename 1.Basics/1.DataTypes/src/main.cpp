#include <stdio.h>

int main(){

   printf("size of char*: %d bytes.\n", sizeof(char*));
   printf("size of int*: %d bytes.\n", sizeof(int*));
   printf("size of float*: %d bytes.\n", sizeof(float*));
   
   return 0;
}